import styled from '@emotion/styled';

export const Pre = styled.pre`
  padding: 2rem;
  font-size: 1.6rem;
  text-align: left;
  border-radius: 10px;
  overflow: scroll;

  & .token-line {
    line-height: 1.3em;
    height: 1.3em;
  }
`;
